import * as React from "react";

import Layout from "../layout/Layout";

export default class SkillList extends React.Component<any> {
    render() {
        return (
            <Layout currentKey="skills">
                <h1>Skill List</h1>
            </Layout>
        );
    }
}
